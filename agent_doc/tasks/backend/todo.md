It is necessary to prepare the application for development 
1) Make a wrapper for responses in the base controller
2) You need to make an error handler and send them via the API in a single format.
3) Prepare folders for Request, Resource, Services, filters
4) Set up authorization via JWT (middleware, etc.)